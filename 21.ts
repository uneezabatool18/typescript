let colors = ["red", "green", "blue"];

// Accessing an element at an invalid index
console.log(colors[3]); // This will produce an index error

console.log(colors[2]); // Accessing the last element of the array
