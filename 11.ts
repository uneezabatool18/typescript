let fname = ["Taha", "Shaka", "Umar", "Hamza"];
let greetingMessage = "Hello, ";

// Printing a personalized message to each person
for (let i = 0; i < fname.length; i++) {
    console.log(greetingMessage + fname[i] + "!");
}
